```{r}
library(ggplot2)
library(gggenes)
library(seqinr)
przyklad <- read.fasta("przyklad2.fasta")
przyklad <- przyklad$NC_003888.3
ggplot(przyklad_frame, aes(xmin = start, xmax = end, y = molecule, fill = gene)) +
  geom_gene_arrow() +
  facet_wrap(~ molecule, scales = "free", ncol = 1) +
  scale_fill_brewer(palette = "Set3")

```
```{r}
library(gggenes)

asdfg <- data.frame(example_genes)

asdfghg <- data.frame(data)
```


```{r}
przyklad_frame <- data.frame(przyklad)
```

```{r}
ggplot(dane, aes(xmin = 1000, xmax = 1100, y = rodzaj, fill = )) +
    geom_gene_arrow()
```
```{r}
plot_data <- read.csv("datasets/data_hupA_chipseq_edgeR.txt", sep=" ") 
ggplot(plot_data, aes(xmin = start, xmax = end, y = rodzaj, fill = )) +
    geom_gene_arrow()
```


